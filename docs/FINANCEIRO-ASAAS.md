# Financeiro Asaas

Estado obrigatório: provedor `asaas`, `habilitado: false`, ambiente `desativado`, cobrança automática `false`. Não há cliente HTTP, segredo ou chamada externa.

Futuro: sandbox antes de produção; cliente, cobrança, assinatura, Pix, boleto, link, webhook autenticado, idempotência, conciliação, histórico e controle manual. Produção depende de ativação explícita, credenciais fora do Git e aceite. Desativado é fail-closed.
