# Orçamento de desempenho V2

Automático: shell público (HTML+CSS+JS) até 100 KiB sem compressão; imagens lazy; nenhum `preload="auto"`. Build registra bytes reais. Homologação deve medir Lighthouse/DevTools nas larguras 360/390/412/430/tablet/desktop, rede móvel e desktop; registrar bytes, requests, LCP, CLS e INP. Não há meta declarada como atingida sem relatório capturado. Cards devem referenciar miniaturas WebP; mídia audiovisual só por interação; requests devem restringir cidade/diretório/shard.
