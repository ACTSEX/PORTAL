import {args,load,validate,convert,report,hash,die} from './migracao-lib.mjs';const a=args(),rows=await load(a.input),issues=validate(rows);if(issues.length)die('Conversão bloqueada: valide o relatório');const converted=convert(rows),checkpoint={schemaVersion:2,sourceHash:hash(rows),outputHash:hash(converted),estado:'convertido_nao_publicado',rollback:{acao:'descartar_saida_local'},records:converted};const summary={modo:a.mode,total:converted.length,publicado:false,checkpointHash:checkpoint.outputHash};console.log(JSON.stringify(summary,null,2));if(a.mode==='convert')console.log(`Saída: ${await report('checkpoint-v1-v2',checkpoint,a.output)}`);
