import {args,load,report,hash,die} from './migracao-lib.mjs';const a=args();if(!a.target)die('Informe --target <checkpoint V2>');const v1=await load(a.input),v2=await load(a.target),rows=v2.records||v2;const result={modo:a.mode,iguais:v1.length===rows.length&&v1.every((x,i)=>x.clienteId===rows[i]?.clienteId&&x.diretorio===rows[i]?.diretorio),v1:v1.length,v2:rows.length,hashV1:hash(v1),hashV2:hash(rows)};console.log(JSON.stringify(result,null,2));if(a.mode==='convert')await report('comparacao-v1-v2',result,a.output);if(!result.iguais)process.exitCode=2;
