import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('público usa lazy loading e vídeo não tem preload automático',async()=>{const html=await readFile('frontend/publico/index.html','utf8'),js=await readFile('frontend/publico/site.js','utf8');assert.match(`${html}${js}`,/loading=['"]lazy|loading:\s*'lazy'/);assert.doesNotMatch(`${html}${js}`,/preload=['"]auto/)});
test('CSS possui breakpoint móvel e bundle está dentro do orçamento medido',async()=>{const css=await readFile('frontend/publico/site.css','utf8');assert.match(css,/@media/);let bytes=0;for(const f of ['frontend/publico/index.html','frontend/publico/site.css','frontend/publico/site.js'])bytes+=(await readFile(f)).length;assert.ok(bytes<=100000,`bundle público ${bytes} excede 100KB`) });
