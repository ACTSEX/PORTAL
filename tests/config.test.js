import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
const load=async p=>JSON.parse(await readFile(p));test('há exatamente três diretórios e temas',async()=>{const c=await load('config/diretorios.json');assert.deepEqual(c.diretorios.map(x=>x.id),['mulheres','homens','transex']);assert.deepEqual(c.diretorios.map(x=>x.tema),['pink','royal','lilás'])});test('categorias não foram inventadas',async()=>{const c=await load('config/categorias.json');assert.deepEqual(c.porDiretorio,{mulheres:[],homens:[],transex:[]})});test('Asaas está desligado',async()=>{const f=await load('examples/financeiro.json');assert.deepEqual([f.habilitado,f.ambiente,f.cobrancaAutomatica],[false,'desativado',false])});
